import React from "react";

const Itinerary = ({ match }) => {
  /* match is a propertie to access the id thats being passed in from the url! */
  return <div> This is where the Itinerary would go (parent component) </div>;
};

export default Itinerary;
